const sports = ['Football', 'Basket', 'Swimming', 'Marathon', 'Fishing'];

console.log(sports.length);

for (const sport of sports) { // Looping strings
  console.log(sport);
}

const numbers = [1, 5, 3, 2, 6];

for (const number of numbers) { // Looping numbers
  console.log(number);
}